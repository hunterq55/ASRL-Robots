function plot_exp(trajectory,reference,error,time)

ts=time/length

for i=1:3
    figure
    hold on
    plot(trajectory(:,i))
    plot(reference(:,i))
end

for i=1:3
    figure
    hold on
    plot(error(:,i))
end

end